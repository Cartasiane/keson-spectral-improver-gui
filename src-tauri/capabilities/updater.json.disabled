{
  "$schema": "https://schemas.tauri.app/capability/2",
  "identifier": "updater-capability",
  "description": "Updater permissions - only for production builds with with-updater feature",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["updater:default", "process:allow-restart"]
}
